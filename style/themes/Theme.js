import amber from '@material-ui/core/colors/amber';

const font = "IRANSans";

const Theme = {
    direction: 'rtl',
    typography: {
        fontFamily: font,
    },
    palette: {
        type: 'light',
        primary: amber,
    },
}

export default Theme;