import indigo from '@material-ui/core/colors/indigo';

const font = "IRANSans";

const Theme = {
    direction: 'rtl',
    typography: {
        fontFamily: font,
    },
    palette: {
        type: 'light',
        primary: indigo,
    },
}

export default Theme;